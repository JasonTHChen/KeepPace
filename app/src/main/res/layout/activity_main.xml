<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/my_toolbar"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="ca.sclfitness.keeppace.MainActivity">

    <Button
        android:id="@+id/btn_main_grindPace"
        android:layout_gravity="center"
        android:layout_width="250dp"
        android:layout_height="50dp"
        android:background="@drawable/button"
        android:fontFamily="Verdana San"
        android:onClick="sendToGrind"
        android:text="@string/main_grindBtn"
        android:textColor="#FFFFFF"
        android:textSize="18dp"
        android:layout_marginBottom="188dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:id="@+id/btn_main_racePace"
        android:layout_gravity="center"
        android:layout_width="250dp"
        android:layout_height="50dp"
        android:background="@drawable/button"
        android:onClick="sendToRace"
        android:text="@string/main_raceBtn"
        android:textColor="#FFFFFF"
        android:layout_marginBottom="84dp"
        android:textSize="18dp"
        android:layout_alignBottom="@+id/btn_main_grindPace"
        android:layout_alignStart="@+id/btn_main_grindPace" />

</RelativeLayout>
